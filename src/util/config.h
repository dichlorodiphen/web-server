#define PORT "80" /* The port to listen for connections on. */

#define MAX_QUEUED_CONNECTIONS                                              \
    10 /* The maximum number of queued connections to allow on the listener \
          socket at once. */